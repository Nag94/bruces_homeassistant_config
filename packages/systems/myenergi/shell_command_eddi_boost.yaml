shell_command:
  eddi_boost: "curl --digest -u {{myenergi_serial}}:{{myenergi_password}} -H 'accept: application/json' -H 'content-type: application/json' --compressed 'https://s7.myenergi.net/cgi-eddi-boost-E{{eddi_serial}}-{{boost_mode}}-{{heater}}-{{boost_duration}}'"


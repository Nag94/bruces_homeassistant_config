template:
  binary_sensor:
    - name: "Paddy Below Max Charge Cost"
      unique_id: paddy_below_max_charge_cost
      availability:  "{{ states('sensor.octopus_agile_current_rate') != 'unknown' }}"        
      state: "{{ (states('sensor.octopus_agile_current_rate') | float) <= (states('input_number.paddy_charge_below_x_pence') | float) }}"

template:
  sensor:
    - name: "Powerwall Efficiency"
      state: "{{ (100 * (states('sensor.powerwall_battery_export')|float / (states('sensor.powerwall_battery_import')|float - states('sensor.powerwall_stored_energy') | float))) | round(1) }}"
      unit_of_measurement: "%"
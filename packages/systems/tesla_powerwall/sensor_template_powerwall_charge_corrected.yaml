template:
  sensor:
    - name: "Powerwall Charge Corrected"
      unique_id: powerwall_charge_corrected
      unit_of_measurement: '%'
      state: "{{ (((states('sensor.powerwall_charge') | float) * 1.05) - 5.0 ) | int  }}"
      device_class: battery
sensor:
  - platform: template
    sensors:
      jyggy_charge_limit_soc:
        value_template: "{{ state_attr('sensor.jyggy_sensor','charge_limit_soc') }}"
        friendly_name: "Charge Limit SOC"
        device_class: battery
        unit_of_measurement: '%'
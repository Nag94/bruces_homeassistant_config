template:
  sensor:
    - name: "Electricity Demand Rounded"
      unique_id: electricity_demand_rounded
      unit_of_measurement: 'kW'
      device_class: power
      state:  "{{ ((states('sensor.import_power') | float )/ 1000.0) | round(1) }}"
template:
  binary_sensor:
    - name: "Elec Voltage Out of Spec"
      unique_id: elec_voltage_out_of_spec
      state: "{{ (states('sensor.apc_ups_input_voltage') | float > 253.0) or (states('sensor.apc_ups_input_voltage') | float < 216.2) }}"
      device_class: problem
          
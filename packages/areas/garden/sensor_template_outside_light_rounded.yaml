template:
  sensor:
    - name: "Outside Light Rounded"
      unique_id: outside_light_rounded
      state: "{{ states('sensor.outside_light') | int }}"
      device_class: "illuminance"
      unit_of_measurement: "lx"